interface:
  display_name: "Tinyvoice"
  short_description: "Repo-specific patterns and workflows for tinyvoice"
  default_prompt: "Use the tinyvoice repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true